🚀 Jenkins CI/CD Pipeline 

Un projet Vagrant automatisé pour déployer un serveur Jenkins sur Ubuntu (focal64) avec Java 17.
Objectif : mettre en place une pipeline CI/CD complète pour la livraison continue de projets.

📦 Technologies utilisées :

*🐧 Ubuntu 20.04 (focal64)

*☕ OpenJDK 17

*⚙️ Jenkins LTS

*📦 Vagrant & VirtualBox

*🐙 Git & GitHub

🔧 Installation rapide

1.Cloner le dépôt

git clone https://github.com/<ton-user>/Jenkins-CI-CD-Pipeline.git
cd Jenkins-CI-CD-Pipeline

2.Démarrer la VM avec Jenkins

vagrant up

3.Accéder à Jenkins

Ouvre ton navigateur à l'adresse :

http://192.168.56.20:8080
Le mot de passe d’administration Jenkins est généré dans :
/var/lib/jenkins/secrets/initialAdminPassword

📁 Structure du projet :

Jenkins-CI-CD-Pipeline/
├── Vagrantfile             # Provisionning automatique (Ubuntu + Java + Jenkins)
├── Jenkinsfile             # Pipeline Jenkins (défini dans le repo)
├── setup/                  # (optionnel) Scripts ou fichiers d'installation
├── .gitignore              # Fichiers à ignorer dans le dépôt
├── README.md               # Documentation du projet
└── notes.md                # Journal personnel (erreurs, commandes, étapes)

✅ Pipeline prévue :

🛠️ Build du projet

🧪 Lint & tests automatisés

🐳 (optionnel) Déploiement Docker ou SSH

📦 Notifications ou reporting

📌 Notes :
Ce projet est un point de départ pour apprendre à configurer Jenkins dans un environnement local.

Tu peux ajouter des jobs supplémentaires dans Jenkins une fois lancé.

